export enum env {
  // 运行环境是dev还是build
  swordCommand = '_SWORD_COMMAND',
  // 运行平台
  swordPlatform = '_SWORD_PLATFORM',
  // cli config配置对象
  swordConfig = '_SWORD_CONFIG'
}

export enum UnicloudEnv {
  symlinkSourcePath = '_unicloud_shim_symlink_source_path'
}
